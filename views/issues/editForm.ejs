<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Issue</title>
</head>

<body>
    <form action="/issues/<%=issue._id%>?_method=PUT" method="POST">
        <!-- <div>
            <label for="title">Title</label>
            <input type="text" id="title" name="issue[title]">
        </div> -->
        <!-- cannot update title -->
        <h1>Update Issue : <%= issue.title %> </h1>
        <div>
            <label for="description">Description</label>
            <input type="text" id="description" name="issue[description]" value="<%=issue.description%>">
        </div>
        <div>
            <label for="priority">Priority</label>
            <input type="text" id="priority" name="issue[priority]" placeholder="High, Medium, Low"
                value="<%= issue.priority %>">
        </div>
        <div>
            <label for="status">Status</label>
            <input type="text" id="status" name="issue[status]" placeholder="Unassigned, Assigned Resolved"
                value="<%= issue.status %>">
        </div>
        <div>
            <label for="assigned_to">Assigned To</label>
            <input type="text" id="assigned_to" name="issue[assigned_to]" value="<%= issue.assigned_to %>">
        </div>
        <!-- inclue modified date and modified by here -->
        <!-- if status is assigned then fill this column -->
        <!-- not asked during input, job of admin to assign these values -->
        <button>Update Issue</button>
    </form>
    <a href="/issues/<%=issue._id%>">Back To Issue</a>
</body>

</html>